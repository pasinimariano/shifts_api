{
    "description": "Response JSON schema for SpecialityController update",
    "type": "object",
    "properties": {
        "speciality_id": {
            "type": "integer"
        },
        "spec_name": {
            "type": "string",
            "minLength": 2,
            "maxLength": 180
        }
    },
    "required": [ "speciality_id", "spec_name" ]
}